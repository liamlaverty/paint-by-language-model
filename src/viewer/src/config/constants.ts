/**
 * Shared configuration constants for the Next.js application.
 *
 * These values are used in both build-time configuration (next.config.ts)
 * and runtime code (basePath.ts).
 */

/**
 * The base path for GitHub Pages deployment.
 * Used when deploying to https://liamlaverty.github.io/paint-by-language-model/
 */
export const GITHUB_PAGES_BASE_PATH = '/paint-by-language-model';
